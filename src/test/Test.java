package test;

public class Test {

	// private static final boolean READ_NOT_WRITE = true;

	// private static final int FILES_TO_CREATE = 5;

	public static void main(String[] args) throws Exception {
		/*
		 * GrandExchange ge = new GrandExchange(); // Made some change // Another change if (READ_NOT_WRITE) { ge.read(); } else { int counter = FILES_TO_CREATE;
		 * 
		 * while (counter-- > 0) { ge.write(new Product(UUID.randomUUID().toString(), "Jason", ProductType.BUY, ProductState.PENDING, (byte) 0, 4151, 1, 0, 1, 0, 1000000,
		 * System.currentTimeMillis())); } }
		 */

	}
}
